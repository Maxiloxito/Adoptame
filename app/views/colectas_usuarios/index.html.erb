<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bienvenido a AdoptaMe</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Baloo+2:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            display: flex;
            flex-direction: column;
            font-family: 'Baloo 2', cursive;
            background-color: #f0f8ff;
            color: #333;
            min-height: 100vh;
        }
        a {
          text-decoration: none;
        }
        header {
            background-color: #add8e6;
            color: #fff;
            padding: 20px 0;
            text-align: center;
        }

        header h1 {
            margin: 0;
            font-weight: 700;
        }

        .container {
            padding: 20px;
        }

        .my-2 {
            margin-bottom: 20px;
        }

        .btn-custom {
            font-family: 'Baloo 2', cursive;
            color: #fff;
            background-color: #ff69b4; /* Rosa suave para el botón de acción principal */
            border: none;
            border-radius: 25px; /* Esquinas redondeadas para un aspecto suave */
            padding: 10px 20px; /* Padding generoso para hacer el botón más grande */
            margin-right: 10px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Sombra suave para profundidad */
        }

        .btn-custom:hover {
            background-color: #fc8da1; /* Un rosa más claro para el hover */
            box-shadow: 0 6px 8px rgba(0, 0, 0, 0.15); /* Sombra más pronunciada en hover */
            transform: translateY(-2px); /* Pequeño desplazamiento hacia arriba para efecto de elevación */
        }

        /* Estilo para el botón de editar */
        .btn-edit {
            background-color: #add8e6; /* Azul suave para el botón de editar */
        }

        /* Estilo para el botón de eliminar */
        .btn-delete {
            background-color: #f08080; /* Rojo suave para el botón de eliminar */
        }

        /* Iconos para los botones, por ejemplo usando Font Awesome */
        .btn-custom .fa {
            margin-right: 5px; /* Espacio entre el icono y el texto */
        }

        /* Estilos adicionales para tu progreso de colectas */
        .progress {
            background-color: #e9ecef; /* Fondo gris claro para la barra de progreso */
            border-radius: 25px; /* Esquinas redondeadas para la barra de progreso */
        }

        .progress-bar {
            background-color: #94d2bd; /* Verde agua para la barra de progreso llena */
            border-radius: 25px; /* Esquinas redondeadas para la parte llena de la barra de progreso */
            animation-name: progress;
            animation-duration: 2s; /* Duración de la animación */
            animation-timing-function: ease-out; /* Efecto de desaceleración al final de la animación */

        }

        .footer {
            background-color: #add8e6;
            color: white;
            padding: 10px;
            text-align: center;
            margin-top: auto;

        }
        @keyframes progress {
          0% { width: 0; }
        }
    </style>
</head>
<body>
    <header>
        <h1>Colectas Activas</h1>
    </header>

    <div class="container my-4">
      <% @colectas.each do |colecta| %>
        <div class="my-2 p-3 border rounded shadow bg-white">
          <h2><%= colecta.nombre %></h2>
          <p><%= colecta.descripcion %></p>
          <% fondos_recaudados = colecta.fondos_recaudados || 0 %>
          <% meta = colecta.meta || 1 %>
          <div class="progress my-2" style="height: 20px;">
            <div class="progress-bar" id="progress-bar-<%= colecta.id %>" role="progressbar" 
                style="width: <%= [fondos_recaudados / meta * 100, 100].min %>%;" 
                aria-valuenow="<%= fondos_recaudados %>" 
                aria-valuemin="0" 
                aria-valuemax="<%= meta %>">
            <%= number_to_currency(fondos_recaudados) %> / <%= number_to_currency(meta) %>
          </div>
          </div><br>
          <%= link_to 'Colaborar', 
                      "https://wa.me/#{+56991620489}?text=Hola, deseo colaborar con la colecta: #{colecta.nombre}", 
                      class: "btn-custom", 
                      target: "_blank" %>
        </div>
      <% end %>
    </div>

    <footer class="footer">
        ❤️ Adopta con responsabilidad
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
    $(document).ready(function(){
        $('.update-amount').on('click', function() {
            var colectaId = $(this).data('colecta-id');
            var newAmount = prompt('Ingrese la nueva cantidad recaudada:');
            
            $.ajax({
              url: '/organizaciones/' + colectaId + '/colectas',
              method: 'PATCH',
              data: { fondos_recaudados: newAmount },
              success: function(response) {
                // Actualiza la barra de progreso
                $('#progress-bar-' + colectaId).css('width', response.percentage + '%');
              }
            });
        });
    });
    </script>
</body>
</html>