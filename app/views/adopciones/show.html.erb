<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles de la Adopción</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Baloo 2', cursive;
            background-color: #f0f8ff;
            color: #333;
        }

        .container {
            padding: 20px;
            max-width: 600px;
            margin: auto;
            background-color: white;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            margin-top: 50px;
        }

        h1 {
            color: #ff69b4; 
            text-align: center;
        }

        p {
            color: #333;
            font-size: 16px;
        }

        .btn-custom {
            background-color: #add8e6;
            color: white;
            border: none;
            border-radius: 20px;
            padding: 10px 20px;
            margin: 5px;
            font-family: 'Baloo 2', cursive;
            transition: background-color 0.3s ease;
        }

        .btn-custom:hover {
            background-color: #7aaed8;
        }

        .whatsapp-link {
            display: inline-block;
            background-color: #25d366;
            color: white;
            padding: 10px 15px;
            border-radius: 20px;
            text-decoration: none;
            margin-top: 15px;
            font-family: 'Baloo 2', cursive;
        }

        .whatsapp-link:hover {
            background-color: #128c7e;
        }
    </style>
</head>
<body>
    <div class="container">
        <%= link_to 'Regresar', :back, class: 'btn-custom' %>
        <h1><%= @adopcion.animal.nombre %></h1>
        <p><%= @adopcion.animal.descripcion %></p>
        <p><%= @adopcion.estado %></p>
        <p>Solicitado por: <%= @adopcion.usuario.nombre %></p>
        <p>Razón: <%= @adopcion.razon %></p>
        <p>Experiencia: <%= @adopcion.experiencia %></p>
        <%= button_to 'Aceptar', accept_animal_adopcione_path(@adopcion.animal, @adopcion), method: :patch, class: 'btn-custom' %>
        <%= button_to 'Rechazar', reject_animal_adopcione_path(@adopcion.animal, @adopcion), method: :patch, class: 'btn-custom' %>
        <%= link_to 'Contactar por WhatsApp', "https://wa.me/#{+56991620489}", class: 'whatsapp-link' %>
    </div>
</body>
</html>
